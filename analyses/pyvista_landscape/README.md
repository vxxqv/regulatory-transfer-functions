# Three-dimensional transfer landscape

The frozen S24 analysis maps complete-state targets onto cis strength, cross-fitted transfer gain, and state-specific module rerouting. Missing rerouting is retained and never assigned a plotting coordinate. Bootstrap, permutation, trajectory selection, camera, and export settings are fixed in `config/pyvista_transfer_landscape.yaml`.
