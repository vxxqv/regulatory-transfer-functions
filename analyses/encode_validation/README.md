# ENCODE CRISPRi validation

The held-out regulatory-element benchmark uses only connections passing the source validity flag. Target genes define cross-validation groups. A distance and assay baseline is compared with a model augmented by CD4 perturbational transfer summaries. Because nearly all rows derive from K562, performance is reported as a technical cross-system benchmark rather than immune-cell replication.
