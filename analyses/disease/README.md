# Disease convergence

The analysis reuses the GSE314342 cluster-level autoimmune enrichment results as disease support, not independent discovery. Prediction uses only real disease rows and holds out entire response clusters. Transfer features are frozen before disease labels enter. Negative-control diseases remain a separate calibration check.
