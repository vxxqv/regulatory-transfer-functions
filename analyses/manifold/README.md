# Response manifold

UMAP and PHATE are fitted to the 30-component global descriptive response basis. Target identity remains grouped during state-prediction validation. The embedding is descriptive and is not used to assign transfer classes or test disease enrichment.
