# Signed response-vector analysis

This stage decomposes cis-normalized, significant trans-response vectors into regulatory modules. Target-level cross-fitting keeps every perturbation target out of the decomposition used to score it. Outputs include Network Gain Decomposition summaries, a target by condition by module transfer tensor, and paired context-rerouting metrics.
